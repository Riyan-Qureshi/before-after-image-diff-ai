## Required Dependencies / Packages
`uv add langgraph python-dotenv notebook`

### For access to chat models
`uv add langchain`

### For Google Gemini
`uv add lang`

## Example dotenv (.env) file contents
`GOOGLE_API_KEY=your-key`